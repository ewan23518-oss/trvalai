# AI Services

This folder contains prompt templates and a reusable itinerary generation module.

- `prompts/itinerary_prompt.py`: structured prompt template
- `engine.py`: LLM integration wrapper with fallback mock generator

Use this module when extracting AI into an independent service later.
